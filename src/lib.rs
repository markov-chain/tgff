//! The package provides a parser for the
//! [TGFF](http://ziyang.eecs.umich.edu/~dickrp/tgff/) (Task Graphs For Free)
//! format, which is a format for storing task graphs and accompanying data
//! used in scheduling and allocation research.

#![feature(macro_rules, if_let)]

use std::collections::HashMap;
use std::iter::Peekable;
use std::str::CharOffsets;

pub use content::Content;
pub use content::{Graph, Task, Arc, Deadline};
pub use content::{Table, Column};

mod content;

static READ_CAPACITY: uint = 20;

pub type Result<T> = std::result::Result<T, Error>;

pub struct Error {
    line: uint,
    message: &'static str,
}

impl std::fmt::Show for Error {
    fn fmt(&self, formatter: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(formatter, "{} on line {}", self.message, self.line)
    }
}

pub struct Parser<'a> {
    line: uint,
    cursor: Peekable<(uint, char), CharOffsets<'a>>,
    content: Content,
}

macro_rules! raise(
    ($parser:expr, $message:expr) => (
        return Err(Error { line: $parser.line, message: $message });
    );
)

impl<'a> Parser<'a> {
    /// Create a new `Parser` for processing the content of a TGFF file
    /// generated by the `tgff` command-line utility and given in `input`.
    pub fn new(input: &'a str) -> Parser<'a> {
        Parser {
            line: 1,
            cursor: input.char_indices().peekable(),
            content: Content::new(),
        }
    }

    /// Perform parsing of the data passed to `new`.
    pub fn process<'a>(&'a mut self) -> Result<&'a Content> {
        loop {
            match self.peek() {
                Some('@') => try!(self.process_at()),
                Some(_) => raise!(self, "found an unknown statement"),
                None => return Ok(&self.content),
            }
        }
    }

    #[inline]
    fn peek(&mut self) -> Option<char> {
        match self.cursor.peek() {
            Some(&(_, c)) => Some(c),
            None => None,
        }
    }

    #[inline]
    fn skip(&mut self) {
        self.next();
    }

    fn skip_void(&mut self) {
        loop {
            match self.peek() {
                Some(c) => match c {
                    ' ' | '\t' | '\n' => self.skip(),
                    _ => break,
                },
                None => break,
            }
        }
    }

    fn process_at(&mut self) -> Result<()> {
        self.skip(); // @

        let name = match self.read_token() {
            Some(name) => name,
            None => raise!(self, "found an @-statement without a name"),
        };

        self.skip_void();

        let number = match self.read_number() {
            Some(number) => number,
            None => raise!(self, "found an @-statement without a number"),
        };

        self.skip_void();

        if let Some('{') = self.peek() {
            self.process_block(name, number)
        } else {
            self.content.set_attribute(name, number);
            Ok(())
        }
    }

    fn process_block(&mut self, _: String, _: String) -> Result<()> {
        self.skip(); // {

        self.skip_void();

        if let Some('#') = self.peek() {
            try!(self.process_table());
        } else {
            try!(self.process_graph());
        }

        self.skip_void();

        if let Some('}') = self.peek() {
            self.skip();
            return Ok(());
        }

        raise!(self, "cannot find the end of a {}-block");
    }

    fn process_graph(&mut self) -> Result<()> {
        loop {
            self.skip_void();

            match self.read_token() {
                Some(token) => match token.as_slice() {
                    "PERIOD" => {},
                    "TASK" => {},
                    "ARC" => {},
                    "HARD_DEADLINE" => {},
                    _ => raise!(self, "found an unknown token in a graph"),
                },
                None => break,
            }
        }

        Ok(())
    }

    fn process_table(&mut self) -> Result<()> {
        Ok(())
    }

    fn read(&mut self, accept: |uint, char| -> bool) -> Option<String> {
        let mut result = std::string::String::with_capacity(READ_CAPACITY);
        let mut i = 0;

        loop {
            match self.peek() {
                Some(c) => {
                    if !accept(i, c) { break; }
                    result.push(c);
                    self.skip();
                    i += 1;
                },
                None => break,
            }
        }

        if i == 0 {
            None
        } else {
            Some(result)
        }
    }

    fn read_token(&mut self) -> Option<String> {
        self.read(|i, c| {
            match c {
                'A'...'Z' | 'a'...'z' if i == 0 => true,
                'A'...'Z' | 'a'...'z' | '_' | '0'...'9' if i > 0 => true,
                _ => false,
            }
        })
    }

    #[inline]
    fn read_number(&mut self) -> Option<String> {
        self.read(|_, c| c >= '0' && c <= '9')
    }
}

impl<'a> std::iter::Iterator<char> for Parser<'a> {
    fn next(&mut self) -> Option<char> {
        match self.cursor.next() {
            Some((_, '\n')) => {
                self.line += 1;
                Some('\n')
            },
            Some((_, c)) => Some(c),
            None => None,
        }
    }
}

#[cfg(test)]
mod tests {
    macro_rules! ok(
        ($result: expr) => (
            if let Err(err) = $result {
                assert!(false, "{}", err);
            }
        );
    )

    macro_rules! error(
        ($result: expr) => (
            if let Ok(_) = $result {
                assert!(false, "expected an error");
            }
        );
    )

    macro_rules! parser(
        ($input:expr) => (super::Parser::new($input));
    )

    #[test]
    fn skip_void() {
        let mut parser = parser!("  \t  abc");
        parser.skip_void();
        assert_eq!(parser.next().unwrap(), 'a');
    }

    #[test]
    fn process_at() {
        ok!(parser!("@abc 12").process_at());
        error!(parser!("@ ").process_at());
        error!(parser!("@abc").process_at());
    }

    #[test]
    fn process_block() {
        ok!(parser!("{}").process_block(String::from_str("foo"),
                                        String::from_str("bar")));
    }

    #[test]
    fn process_graph() {
        ok!(parser!("  TASK t0_0\tTYPE 2   ").process_graph());
    }

    #[test]
    fn process_table() {
    }

    #[test]
    fn read_token() {
        macro_rules! test(
            ($input:expr, $output:expr) => (
                assert_eq!(parser!($input).read_token().unwrap(),
                           String::from_str($output));
            );
        )

        test!("AZ xyz", "AZ");
        test!("az xyz", "az");
        test!("AZ_az_09 xyz", "AZ_az_09");
    }
}
